#include <iostream>
using namespace std;

int main()
{
    double length, area;

    cout << "Enter length : ";
    cin >> length;

    area = (length * length); //logic : area of square

    cout << "Area of square = " << area;

    return 0;
}
